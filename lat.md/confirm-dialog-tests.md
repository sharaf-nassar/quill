---
lat:
  require-code-mention: true
---

# Confirmation Dialog Tests

These specs keep shared confirmation dialogs inside the webview that owns the triggering action.

## Manage Window Centering

A provider confirmation opened from Settings uses the active Manage viewport's horizontal and vertical center, regardless of the host window's screen position.
